---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
	title?: string;
	description?: string;
}

const { title = "sitecue - Capture your thoughts instantly", description = "Capture your thoughts instantly, right on the spot. sitecue pins your memos directly to the web pages you are working on." } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body class="bg-white antialiased">
    <Header />
    <slot />
    <Footer />
  </body>
</html>
